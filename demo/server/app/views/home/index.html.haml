- Post.all.reverse.each do |post|
	.container

		.date= post.created_at.strftime("%b %e, %Y")
		.author= post.author
		said,
		.comment
			="“#{post.content}”"
		
		-if !post.responses.empty?
			.divider
				%hr
		
			.response
				-post.responses.each do |response|
					.author= response.author
					replied,
					.comment
						="“#{response.content}”"